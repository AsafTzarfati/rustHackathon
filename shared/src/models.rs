// Shared domain models (if any additional ones are needed beyond proto)
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AppStateSnapshot {
    // We will populate this later
}
